{{ form_start(contactForm) }}
    {{ form_row(contactForm.sujetContact) }}
    {{ form_row(contactForm.messageContact) }}
    <div class="user-selection">
    {{ form_row(contactForm.idUtilContact) }}
    </div>
<button type="submit">Envoyer</button>
{{ form_end(contactForm) }}